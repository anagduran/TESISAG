#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babel-register-cli/bin/babel-register" "$@"
  ret=$?
else 
  node  "$basedir/../babel-register-cli/bin/babel-register" "$@"
  ret=$?
fi
exit $ret
